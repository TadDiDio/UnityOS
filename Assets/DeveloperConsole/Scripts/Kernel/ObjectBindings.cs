namespace DeveloperConsole
{
    public class ObjectBindings
    {
        // This will eventually hold a dictionary that allows for <string, object> bindings.
        // Dynamic, interactive binding commands will allow setting these at runtime, and
        // hopefully a compile/boot time solution is found too.
        //
        // TODO: Add this to kernel services eventually.
    }
}